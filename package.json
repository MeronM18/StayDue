{
  "name": "staydue",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter ./apps/web dev",
    "start:project": "pnpm --filter ./apps/web dev",
    "lint": "pnpm -r lint",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "eslint": "^9.12.0",
    "prettier": "^3.3.3"
  },
  "dependencies": {
    "@staydue/lib": "workspace:^",
    "@staydue/ui": "workspace:^"
  }
}
